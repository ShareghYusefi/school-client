import { ColofulBg } from './coloful-bg';

describe('ColofulBg', () => {
  it('should create an instance', () => {
    const directive = new ColofulBg();
    expect(directive).toBeTruthy();
  });
});
